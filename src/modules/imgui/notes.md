Im unsure if this should be a core module, Since it does include the ability to switch to another backend (or a custom one) then i should go without saying; no, since any user from any platform could just include it in their compiled app. 

Ignoring compiled app, it would be called via require anyway, some setup later and user is good to go. examples will be needed as well for new programmers.